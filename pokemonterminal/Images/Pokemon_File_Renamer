import os

# Folder_Path = '/home/mark/Documents/Adams_Dev_Test/Pokemon-Terminal/pokemonterminal/Images/Test'
folderPath = '/home/adam/Pokemon/assets/imagesHQ'


def pokemonImageRenamer(filePath):
    dexNoString = filePath[:4]
    dexNoInt = int(dexNoString)-1
    # namesTxt = open('/home/mark/Documents/Adams_Dev_Test/Pokemon-Terminal/pokemonterminal/Data/pokemon.txt').readlines()
    namesTxt = open('/home/adam/pokemon.txt').readlines()
    chosenLine = namesTxt[dexNoInt]
    pokemonName = chosenLine.split()[0]
    print(pokemonName)
    variant = filePath[4:]
    newFileName = f"{dexNoString}_{pokemonName.capitalize()}{variant}"
    print(newFileName)
    oldName = os.path.join(folderPath, filePath)
    newName = os.path.join(folderPath, newFileName)
    os.rename(oldName, newName)

for fileName in os.listdir(folderPath):
    if fileName.endswith('.png'):
        pokemonImageRenamer(fileName)
